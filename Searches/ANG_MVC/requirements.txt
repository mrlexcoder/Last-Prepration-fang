# ANG MVC v3 — Python dependencies (GPU + CPU compatible)
# NOTE: CUDA torch + bitsandbytes are installed in Dockerfile for GPU builds.
# This file is also used for bare-metal .venv installs.

# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
jinja2==3.1.4
pydantic>=2.0
python-multipart==0.0.9

# Vector memory (GPU version of FAISS is optional; faiss-cpu is safe fallback)
faiss-cpu==1.9.0.post1
numpy>=1.24
sentence-transformers==2.7.0
qdrant-client==1.9.1

# HuggingFace + quantization (bitsandbytes required for 4-bit + 50% limit)
transformers==4.51.3
accelerate==1.7.0
datasets==2.19.0
bitsandbytes==0.43.1

# Web scraping
httpx==0.27.0
trafilatura==1.12.2
selectolax==0.3.21
playwright==1.44.0

# Kafka
aiokafka==0.11.0

# Mem0 memory
mem0ai==0.1.29

# gRPC (Go store client)
grpcio==1.63.0
grpcio-tools==1.63.0
protobuf==5.29.6
