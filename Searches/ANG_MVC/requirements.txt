# ANG MVC — pinned dependencies
# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
jinja2==3.1.4
pydantic==2.7.1
python-multipart==0.0.9

# Vector memory (InfinityCache)
faiss-cpu==1.8.0
numpy==1.26.4

# Embeddings for InfinityCache semantic search
sentence-transformers==3.0.1

# HuggingFace adapter (optional — comment out if not using GPU/HF models)
# transformers==4.41.2
# torch==2.3.0
# accelerate==0.30.1

# llama.cpp Python binding (optional — comment out if using subprocess mode)
# llama-cpp-python==0.2.77
